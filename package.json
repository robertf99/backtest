{
  "name": "backtest",
  "version": "1.0.0",
  "repository": "https://github.com/robertf99/backtest.git",
  "author": "robertf99 <robertf99@gmail.com>",
  "scripts": {
    "docker:build": "docker-compose -f docker-compose.yml --project-directory . build",
    "docker:up": "docker-compose -f docker-compose.yml --project-directory . up -d",
    "docker:down": "docker-compose -f docker-compose.yml --project-directory . down"
  }
}